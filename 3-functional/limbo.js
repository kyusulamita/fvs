
    describe('using curry', () => {

      describe('add', () => {

        it('has been curried', () => {

        });

        it('creates a function that adds the partially applied number', () => {

        });

        it('can add two numbers supplied at once as well', () => {

        });

      }); // end describe('add')

      describe('multiply', () => {

        it('has been curried', () => {

        });

        it('creates a function that multiplies the partially applied number', () => {

        });

        it('can multiply two numbers supplied at once as well', () => {

        });

      }); // end describe('multiply')

      describe('curriedMap', () => {

        it('has been curried', () => {

        });

        it('creates a function that partially applies a predicate, which maps an array with that predicate', () => {

        });

      });

      describe('curriedFilter', () => {

        it('has been curried', () => {

        });

        it('creates a function that partially applies a predicate, which filters an array with that predicate', () => {

        });

      });

      describe('curriedReduce', () => {

        it('has been curried', () => {

        });

        it('creates a function that partially applies a predicate, which creates a function which partially applies an accumulator, which reduces an array with that predicate/accumulator', () => {

        });

      });

    }); // end describe('using curry')
